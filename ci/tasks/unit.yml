---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: python

params:

inputs:
  - name: pcfdemo

run:
  path: pcfdemo/ci/tasks/unit.sh
  args: [
    --input-dir, pcfdemo
  ]
